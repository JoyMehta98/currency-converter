export interface CurrencyResponse {
  id: number;
  name: string;
  symbol: string;
  sign: string;
}

export interface CryptoCurrencyResponse {
  id: number;
  name: string;
  symbol: string;
  slug: string;
}
